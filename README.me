TODOS after Project

• Encypt Data in the Local Storage
• Improve Register Feature: If we register we do not want to instantly log in the user, instead we just return a message saying that the user is registered and the user needs to Login manually in the Login Screen
• Improve Profile Screen -- the update should be a modal for example or another component
• Improve Redirect when attempting to access a profile route. (Using useNavigate of React Router v6)
• CREATE A MODAL AND NOT AN ALERT WHEN DELETING A USER

• IMPROVE ROUTING WITH NESTED ROUTED (ADMIN ROUTE)